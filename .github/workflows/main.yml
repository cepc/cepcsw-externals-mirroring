# Description:
#   Mirroring the packages

name: MirrorExternals

on:
  push:
    branches:
    - master

jobs:
  mirroring:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # All the required packages are list here.
        PACKAGE_URL: 
          - https://gitlab.cern.ch/gaudi/Gaudi.git
          - https://github.com/key4hep/K4FWCore
    steps:
      - name: mirror
        run: |
          echo ${PACKAGE_URL}
          package=$(basename ${PACKAGE_URL})
          git clone ${PACKAGE_URL} ${package}
          cd ${package}
          git remote rename origin old-origin
          git remote add origin ${MIRROR_REPO_PREFIX}/Gaudi.git
          git push --set-upstream origin --all
          
        env:
          PACKAGE_URL: ${{matrix.PACKAGE_URL}}
          MIRROR_REPO_PREFIX: git@code.ihep.ac.cn:lintao

